import { Request, Response, NextFunction } from "express";

export async function verifyAccessToken(
  req: Request,
  res: Response,
  next: NextFunction
) {
  // Assigned to Mariam
  // TODO: Write the logic right here
}
